[workspace]
resolver = "2"
members = ["core", "client/src-tauri", "server"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"

[profile.release]
strip = true
lto = "thin"
